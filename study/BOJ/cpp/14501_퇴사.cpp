#include <iostream>
using namespace std;

pair<int,int> tp[20];
int d[10];

int main(void) {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> tp[i].first >> tp[i].second;
    for (int i = 1; i <= n; i++) {
        
    }
}
