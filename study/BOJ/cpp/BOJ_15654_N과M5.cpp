#include <iostream>
#include <vector>
#include <set>

std::vector<int> v;
std::set<std::vector<int>> result;
int n, m;

int main(void) {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    
}